# 360 Magicians - Python Dependencies
# See filesystem for project structure

# File type identification library
pylibmagic>=0.5.0

# A2A auth chain – Copilot OIDC + MAGI identity provisioning
# Pinned to patched versions; see advisory audits in PR description.
PyJWT>=2.12.0
cryptography>=48.0.1

# Testing
pytest>=8.3.0
